version: "3"

services:
  api:
    env_file:
      - ./server/.env.prod
    image: $AWS_API_IMAGE
    ports:
      - "5001:5001"
    logging:
      driver: awslogs
      options:
        awslogs-group: zigoom-group
        awslogs-region: ap-northeast-2
        awslogs-stream-prefix: api
